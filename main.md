1. Which logs do I need in the future?
Here is the golden rule for which logs to send me:

If the UI crashes, flashes, or the video player spins forever: I need Browser Console Logs + Browser Network Tab screenshots.

If the server throws a 500 error, or no servers are found: I need the Mac Terminal (npm run dev) logs.

If a specific API is returning weird data (like ReAnime just did): I need the Render logs.